name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read   # allow GitHub to read repository contents
  id-token: write  # required for OIDC authentication with Deno Deploy

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      # (ä»»æ) åãã§ãã¯ããã«ãã¿ã¹ã¯ãããã°å®è¡
            # åãã§ãã¯ã¨ãã©ã¼ãããèªåå®è¡
      - name: Format and check
        run: |
          deno fmt
          deno task check

      - name: Deploy to Deno Deploy
        uses: denoland/deployctl@v1
        with:
          project: namnam-cosense-exp-47  # Deno Deploy ä¸ã®ãã­ã¸ã§ã¯ãå
          root: "."                   # ãªãã¸ããªå¨ä½ãã«ã¼ãã¨ãã¦æå®
          entrypoint: main.ts          # å®éã«å®è¡ããããã¡ã¤ã«ãæå®
