name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read # ã½ã¼ã¹ã³ã¼ããã§ãã¯ã¢ã¦ãç¨
  id-token: write # â OIDC ãã¼ã¯ã³çºè¡ãè¨±å¯

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v2.x
      - run: deno task check
      - uses: denoland/deployctl@v1
        with:
          project: cosense-exporter
          entrypoint: server.ts
